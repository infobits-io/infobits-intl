// GENERATED CODE - DO NOT MODIFY BY HAND

package i18n

// {{pascalCase .Name}}Translations contains translations keyed by locale then code
var {{pascalCase .Name}}Translations = map[string]map[string]string{
{{- range $locale, $trans := .Translations}}
	"{{$locale}}": {
{{- range $code, $name := $trans}}
		"{{$code}}": "{{escapeGoString $name}}",
{{- end}}
	},
{{- end}}
}

// Get{{pascalCase .Name}}Name returns the translated name for a code in a locale
func Get{{pascalCase .Name}}Name(code, locale string) (string, bool) {
	if localeMap, ok := {{pascalCase .Name}}Translations[locale]; ok {
		if name, ok := localeMap[code]; ok {
			return name, true
		}
	}
	return "", false
}
